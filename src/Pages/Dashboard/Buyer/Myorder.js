import React from 'react';
import Loader from '../../Shared/Loader/Loader';

const Myorder = () => {
    return (
        <div>
            <Loader></Loader>
        </div>
    );
};

export default Myorder;